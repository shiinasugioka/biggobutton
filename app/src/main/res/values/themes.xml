<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BigGoButton" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BigGoButton" parent="Base.Theme.BigGoButton" />

    <style name="buttonStyle">
        <item name="android:backgroundTint">#0D370A</item>
        <item name="android:text">@string/button_text</item>
        <item name="android:textColor">#D62525</item>
        <item name="android:textSize">40dp</item>
        <item name="android:textStyle">italic</item>
    </style>
</resources>